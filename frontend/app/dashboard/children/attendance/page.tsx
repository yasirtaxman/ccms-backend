"use client";
import Link from "next/link";import { useSearchParams } from "next/navigation";import { DailyAttendanceRegister } from "@/components/children/DailyAttendanceRegister";
export default function AttendancePage(){const search=useSearchParams();const child=search.get("child");return <div className="space-y-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-end"><div className="flex-1"><p className="eyebrow">Daily presence register</p><h1 className="page-title">Daily child attendance</h1><p className="page-subtitle">Track residential presence separately from academic attendance.</p></div><Link href="/dashboard/children/attendance/reports" className="secondary-button">Reports & PDF exports</Link></div><DailyAttendanceRegister initialChild={child?Number(child):undefined}/></div>}
