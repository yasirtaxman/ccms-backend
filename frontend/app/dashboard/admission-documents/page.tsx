import Link from "next/link";
const required=["Admission Form","Child Photo","Birth Certificate / Form-B","Guardian CNIC","Father Death Certificate","Medical Certificate"];
const optional=["School / Education Record","Court / Legal Order","Other Supporting Document"];
export default function AdmissionDocumentsPage(){return <div className="space-y-6"><header><p className="eyebrow">Child management</p><h1 className="page-title">Admission Documents</h1><p className="page-subtitle">Approved admission-file checklist and upload guidance.</p></header><div className="notice-warning">Admission file is incomplete until all required documents are uploaded and verified.</div><div className="grid gap-6 lg:grid-cols-2"><List title="Required documents" items={required}/><List title="Optional supporting documents" items={optional}/></div><section className="panel"><h2 className="text-lg font-bold">Upload workflow</h2><p className="mt-2 text-sm leading-6 text-slate-600">Open a child profile, select an approved document type from the dropdown, choose a PDF/JPG/JPEG/PNG file, and upload it. Authorized staff can then verify the document. Draft status is not changed automatically.</p><div className="mt-5 flex flex-wrap gap-3"><Link href="/dashboard/children" className="primary-button">Open Children</Link><Link href="/dashboard/children/import" className="secondary-button">Import Children</Link></div></section></div>}
function List({title,items}:{title:string;items:string[]}){return <section className="panel"><h2 className="text-lg font-bold">{title}</h2><ul className="mt-4 space-y-2">{items.map(item=><li className="rounded-lg border border-slate-200 px-4 py-3 text-sm font-medium" key={item}>{item}</li>)}</ul></section>}
