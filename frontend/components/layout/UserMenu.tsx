"use client";
import { LogOut, UserRound } from "lucide-react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/hooks/useAuth";
export function UserMenu(){const router=useRouter();const {user,logout}=useAuth();const leave=()=>{logout();router.replace("/login")};return <details className="relative"><summary className="flex cursor-pointer list-none items-center gap-3 rounded-xl border border-slate-200 bg-white px-3 py-2 hover:bg-slate-50"><div className="grid size-9 place-items-center rounded-full bg-blue-100 text-blue-700"><UserRound size={18}/></div><div className="hidden text-left sm:block"><div className="text-sm font-semibold text-slate-800">{user?.username}</div><div className="max-w-40 truncate text-xs text-slate-500">{user?.roles.join(", ")}</div></div></summary><div className="absolute right-0 mt-2 w-64 rounded-xl border border-slate-200 bg-white p-2 shadow-xl"><div className="border-b border-slate-100 px-3 py-2"><p className="text-sm font-semibold">{user?.full_name||user?.username}</p><p className="text-xs text-slate-500">{user?.roles.join(" · ")}</p></div><div className="px-3 py-3 text-sm text-slate-500">Change password will be available in the next frontend phase.</div><button onClick={leave} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-sm font-medium text-red-600 hover:bg-red-50"><LogOut size={16}/>Log out</button></div></details>}
