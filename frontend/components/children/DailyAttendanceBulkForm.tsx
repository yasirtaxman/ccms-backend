"use client";
import { CheckCheck,RotateCcw,Save } from "lucide-react";
export function DailyAttendanceBulkForm({canEdit,busy,onAllPresent,onReset,onSave,selected}:{canEdit:boolean;busy:boolean;onAllPresent:()=>void;onReset:()=>void;onSave:()=>void;selected:number}){if(!canEdit)return <p className="rounded-lg bg-slate-100 px-4 py-3 text-sm text-slate-600">Read-only attendance access</p>;return <div className="flex flex-wrap items-center gap-2"><button className="secondary-button" onClick={onAllPresent}><CheckCheck size={16}/>Mark all Present</button><button className="secondary-button" onClick={onReset}><RotateCcw size={16}/>Reset unsaved</button><button className="primary-button" disabled={busy||selected===0} onClick={onSave}><Save size={16}/>{busy?"Saving…":`Save ${selected} records`}</button></div>}
